{% extends 'base.html.twig' %}

{% block title %}Créer un compte - Sortir.com{% endblock %}

{% block body %}
    <div class="min-h-screen bg-gradient-to-br from-purple-100 via-purple-50 to-white flex items-center justify-center px-4 py-12 relative">
        <div class="absolute inset-0 overflow-hidden">
            <div class="absolute top-10 left-10 text-xl animate-pulse opacity-40">🎉</div>
            <div class="absolute top-20 right-20 text-lg animate-bounce opacity-40">✨</div>
            <div class="absolute bottom-20 left-20 text-xl animate-pulse opacity-40" style="animation-delay: 1s;">🍻</div>
            <div class="absolute bottom-40 right-10 text-lg animate-bounce opacity-40" style="animation-delay: 2s;">📍</div>
            <div class="absolute top-1/2 left-1/3 text-lg animate-pulse opacity-40" style="animation-delay: 3s;">🎯</div>
            <div class="absolute top-1/4 left-1/4 w-40 h-40 bg-purple-600/10 rounded-full blur-2xl"></div>
            <div class="absolute bottom-1/4 right-1/4 w-60 h-60 bg-purple-600/8 rounded-full blur-3xl"></div>
            <div class="absolute top-1/2 right-1/3 w-32 h-32 bg-purple-400/12 rounded-full blur-xl"></div>
        </div>

        <div class="relative z-10 w-full max-w-2xl">
            <div class="text-center mb-8 p-4 bg-white/60 backdrop-blur-sm rounded-xl shadow-lg border border-purple-600/10">
                <h1 class="text-3xl font-bold text-gray-900 mb-2">
                    Rejoignez l'aventure ! 🚀
                </h1>
                <p class="text-gray-600">
                    Créez votre compte et organisez vos premières sorties
                </p>
            </div>

            <div class="bg-white/90 backdrop-blur-sm border border-purple-600/20 rounded-2xl shadow-2xl shadow-purple-600/10 p-8">
                {{ form_start(registrationForm, {'attr': {'class': 'space-y-6'}}) }}

                {% if form_errors(registrationForm) %}
                    <div class="bg-red-50 border border-red-200 text-red-700 px-4 py-3 rounded-lg text-sm">
                        {{ form_errors(registrationForm) }}
                    </div>
                {% endif %}

                <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
                    <div class="space-y-2">
                        <label for="{{ registrationForm.prenom.vars.id }}" class="block text-sm font-medium text-gray-700">
                            Prénom <span class="text-red-500">*</span>
                        </label>
                        {{ form_widget(registrationForm.prenom, {
                            'attr': {
                                'class': 'w-full px-4 py-3 border border-purple-600/30 rounded-lg focus:ring-2 focus:ring-purple-600 focus:border-purple-600 transition-all outline-none text-gray-900 placeholder-gray-500 bg-white shadow-sm focus:shadow-md',
                                'placeholder': 'Votre prénom'
                            }
                        }) }}
                        {% if form_errors(registrationForm.prenom) %}
                            <div class="text-red-600 text-sm flex items-center gap-1">
                                <span>⚠️</span>
                                {{ form_errors(registrationForm.prenom) }}
                            </div>
                        {% endif %}
                    </div>

                    <div class="space-y-2">
                        <label for="{{ registrationForm.nom.vars.id }}" class="block text-sm font-medium text-gray-700">
                            Nom <span class="text-red-500">*</span>
                        </label>
                        {{ form_widget(registrationForm.nom, {
                            'attr': {
                                'class': 'w-full px-4 py-3 border border-purple-600/30 rounded-lg focus:ring-2 focus:ring-purple-600 focus:border-purple-600 transition-all outline-none text-gray-900 placeholder-gray-500 bg-white shadow-sm focus:shadow-md',
                                'placeholder': 'Votre nom'
                            }
                        }) }}
                        {% if form_errors(registrationForm.nom) %}
                            <div class="text-red-600 text-sm flex items-center gap-1">
                                <span>⚠️</span>
                                {{ form_errors(registrationForm.nom) }}
                            </div>
                        {% endif %}
                    </div>
                </div>

                <div class="space-y-2">
                    <label for="{{ registrationForm.email.vars.id }}" class="block text-sm font-medium text-gray-700">
                        Adresse email <span class="text-red-500">*</span>
                    </label>
                    {{ form_widget(registrationForm.email, {
                        'attr': {
                            'class': 'w-full px-4 py-3 border border-purple-600/30 rounded-lg focus:ring-2 focus:ring-purple-600 focus:border-purple-600 transition-all outline-none text-gray-900 placeholder-gray-500 bg-white shadow-sm focus:shadow-md',
                            'placeholder': 'votre@email.com'
                        }
                    }) }}
                    {% if form_errors(registrationForm.email) %}
                        <div class="text-red-600 text-sm flex items-center gap-1">
                            <span>⚠️</span>
                            {{ form_errors(registrationForm.email) }}
                        </div>
                    {% endif %}
                </div>

                <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
                    <div class="space-y-2">
                        <label for="{{ registrationForm.Site.vars.id }}" class="block text-sm font-medium text-gray-700">
                            Site <span class="text-red-500">*</span>
                        </label>
                        {{ form_widget(registrationForm.Site, {
                            'attr': {
                                'class': 'w-full px-4 py-3 border border-purple-600/30 rounded-lg focus:ring-2 focus:ring-purple-600 focus:border-purple-600 transition-all outline-none text-gray-900 bg-white shadow-sm focus:shadow-md'
                            }
                        }) }}
                        {% if form_errors(registrationForm.Site) %}
                            <div class="text-red-600 text-sm flex items-center gap-1">
                                <span>⚠️</span>
                                {{ form_errors(registrationForm.Site) }}
                            </div>
                        {% endif %}
                    </div>

                    <div class="space-y-2">
                        <label for="{{ registrationForm.telephone.vars.id }}" class="block text-sm font-medium text-gray-700">
                            Téléphone
                        </label>
                        {{ form_widget(registrationForm.telephone, {
                            'attr': {
                                'class': 'w-full px-4 py-3 border border-purple-600/30 rounded-lg focus:ring-2 focus:ring-purple-600 focus:border-purple-600 transition-all outline-none text-gray-900 placeholder-gray-500 bg-white shadow-sm focus:shadow-md',
                                'placeholder': '06 12 34 56 78'
                            }
                        }) }}
                        {% if form_errors(registrationForm.telephone) %}
                            <div class="text-red-600 text-sm flex items-center gap-1">
                                <span>⚠️</span>
                                {{ form_errors(registrationForm.telephone) }}
                            </div>
                        {% endif %}
                    </div>
                </div>

                <div class="space-y-2">
                    <label for="{{ registrationForm.plainPassword.vars.id }}" class="block text-sm font-medium text-gray-700">
                        Mot de passe <span class="text-red-500">*</span>
                    </label>
                    {{ form_widget(registrationForm.plainPassword, {
                        'attr': {
                            'class': 'w-full px-4 py-3 border border-purple-600/30 rounded-lg focus:ring-2 focus:ring-purple-600 focus:border-purple-600 transition-all outline-none text-gray-900 placeholder-gray-500 bg-white shadow-sm focus:shadow-md',
                            'placeholder': '••••••••'
                        }
                    }) }}
                    {% if form_errors(registrationForm.plainPassword) %}
                        <div class="text-red-600 text-sm flex items-center gap-1">
                            <span>⚠️</span>
                            {{ form_errors(registrationForm.plainPassword) }}
                        </div>
                    {% endif %}
                    <p class="text-xs text-gray-500">Minimum 6 caractères</p>
                </div>

                <div class="flex items-start space-x-2">
                    {{ form_widget(registrationForm.agreeTerms, {
                        'attr': {
                            'class': 'w-4 h-4 text-purple-600 border-purple-600/40 rounded focus:ring-purple-600 mt-1'
                        }
                    }) }}
                    <label for="{{ registrationForm.agreeTerms.vars.id }}" class="text-sm text-gray-700">
                        J'accepte les <a href="#" class="text-purple-600 hover:underline">conditions d'utilisation</a>
                        et la <a href="#" class="text-purple-600 hover:underline">politique de confidentialité</a>
                        <span class="text-red-500">*</span>
                    </label>
                </div>
                {% if form_errors(registrationForm.agreeTerms) %}
                    <div class="text-red-600 text-sm flex items-center gap-1">
                        <span>⚠️</span>
                        {{ form_errors(registrationForm.agreeTerms) }}
                    </div>
                {% endif %}

                <div class="flex flex-col sm:flex-row gap-3">
                    <button type="submit"
                            class="flex-1 bg-purple-600 text-white font-semibold py-3 rounded-lg hover:bg-purple-700 transition-all duration-200 hover:scale-[1.02] shadow-lg hover:shadow-xl shadow-purple-600/30">
                        Créer mon compte 🎉
                    </button>
                    <a href="{{ path('app_index') }}"
                       class="flex-1 text-center px-4 py-3 border-2 border-gray-300 text-gray-600 font-semibold rounded-lg hover:bg-gray-50 transition-all duration-200 hover:scale-[1.02] bg-white shadow-md">
                        Annuler
                    </a>
                </div>

                {{ form_end(registrationForm) }}

                <div class="mt-6 relative">
                    <div class="absolute inset-0 flex items-center">
                        <div class="w-full border-t border-purple-600/20"></div>
                    </div>
                    <div class="relative flex justify-center text-sm">
                        <span class="px-3 bg-white text-gray-500">ou</span>
                    </div>
                </div>

                <div class="text-center">
                    <p class="text-gray-600">
                        Déjà inscrit ?
                        <a href="{{ path('app_login') }}" class="text-purple-600 hover:underline font-medium">
                            Connectez-vous ici 👋
                        </a>
                    </p>
                </div>
            </div>

            <div class="mt-8 text-center text-sm text-gray-500 p-3 bg-white/40 backdrop-blur-sm rounded-lg shadow-sm border border-purple-600/10">
                <p>
                    🔒 Vos données sont sécurisées et ne seront jamais partagées
                </p>
            </div>
        </div>
    </div>
{% endblock %}
