
<form method="post" class="w-full" action="{{ path('app_participant_delete', {'id': participant.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ participant.id) }}">
    <button class="px-4 py-2 bg-red-300 text-red-600 rounded-lg hover:bg-red-400 transition">Delete</button>
</form>
